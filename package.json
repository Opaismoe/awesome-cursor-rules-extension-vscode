{
  "name": "cursor-rules-extension",
  "displayName": "Cursor Rules Manager",
  "description": "Quickly create and manage CursorRules for the Cursor editor",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:cursor-rules.create",
    "onCommand:cursor-rules.select",
    "onCommand:cursor-rules.edit",
    "onCommand:cursor-rules.selectGithub",
    "onCommand:cursor-rules.selectGithubCached",
    "onCommand:cursor-rules.resetGithubRateLimit"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cursor-rules.create",
        "title": "Cursor Rules: Create New Rule"
      },
      {
        "command": "cursor-rules.select",
        "title": "Cursor Rules: Select from Templates"
      },
      {
        "command": "cursor-rules.edit",
        "title": "Cursor Rules: Edit Existing Rule"
      },
      {
        "command": "cursor-rules.selectGithub",
        "title": "Cursor Rules: Select from GitHub Templates"
      },
      {
        "command": "cursor-rules.selectGithubCached",
        "title": "Cursor Rules: Select GitHub Templates (Faster)"
      },
      {
        "command": "cursor-rules.resetGithubRateLimit",
        "title": "Cursor Rules: Reset GitHub Rate Limit Status"
      }
    ],
    "configuration": {
      "title": "Cursor Rules",
      "properties": {
        "cursorRules.templateSources": {
          "type": "array",
          "default": [
            "https://github.com/PatrickJS/awesome-cursorrules/tree/main/rules"
          ],
          "description": "GitHub repositories to fetch rule templates from"
        },
        "cursorRules.useDirectoryStructure": {
          "type": "boolean",
          "default": true,
          "description": "Use .cursor/rules directory structure instead of .cursorrules file"
        },
        "cursorRules.githubToken": {
          "type": "string",
          "default": "",
          "description": "GitHub personal access token to increase API rate limits"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "test": "node ./out/test/runTest.js"
  },
  "keywords": [
    "cursor",
    "ai",
    "rules",
    "cursorrules"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.11.5",
    "@types/sinon": "^17.0.4",
    "@types/vscode": "^1.60.0",
    "@vscode/test-electron": "^2.3.4",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "sinon": "^15.0.1",
    "typescript": "^5.3.3"
  }
}
